---
import BaseLayout from './BaseLayout.astro';
import Navigation from '../components/Navigation.astro';

// const { frontmatter } = Astro.props;
const { 
    title, 
    description, 
    date, 
    updatedDate, 
    heroImage, 
    category, 
    draft, 
    slug,
} = Astro.props;

---


<BaseLayout {title} {description}>

    <div class="post-content">

    <!--- Blog metadata: Title, Description, Date, Category --->

        <div class="blog-info">
            <h1>{title}</h1>
                <h2>{description}</h2> 
                <h6>{date.toLocaleDateString('en-US', {month: 'long', day: 'numeric', year: 'numeric',})}  •  {category}</h6>
        </div>

    <!--- Blog post (.md) gets slotted in here --->
            <slot />
    </div>

</BaseLayout>

